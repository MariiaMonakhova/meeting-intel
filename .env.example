ANTHROPIC_API_KEY=sk-ant-...

# Optional: enables real semantic embeddings for cross-meeting RAG search
# (get a key at https://voyageai.com). Without it, RAG falls back to a
# free local TF-IDF (word-matching) search - everything else still works.
VOYAGE_API_KEY=voyage-...
